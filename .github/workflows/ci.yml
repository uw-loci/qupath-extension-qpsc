name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'
          cache: 'gradle'

      - name: Checkout tiles-to-pyramid dependency
        uses: actions/checkout@v4
        with:
          repository: uw-loci/qupath-extension-tiles-to-pyramid
          path: tiles-to-pyramid

      - name: Build and publish tiles-to-pyramid to mavenLocal (Linux)
        if: runner.os != 'Windows'
        working-directory: tiles-to-pyramid
        run: |
          chmod +x gradlew
          ./gradlew publishToMavenLocal

      - name: Build and publish tiles-to-pyramid to mavenLocal (Windows)
        if: runner.os == 'Windows'
        working-directory: tiles-to-pyramid
        run: ./gradlew publishToMavenLocal

      - name: Grant execute permission for gradlew
        if: runner.os != 'Windows'
        run: chmod +x gradlew

      - name: Build and test (Linux)
        if: runner.os == 'Linux'
        run: xvfb-run ./gradlew build
        env:
          DISPLAY: ':99'

      - name: Build and test (Windows)
        if: runner.os == 'Windows'
        run: ./gradlew build
